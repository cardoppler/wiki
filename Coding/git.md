- If you commit with the wrong message
- Exclude dir
- Exclude files
- Delete and remove folder

```
git rm -r --cached directory                # If you want to ignore directory but you have already committed it in the past
```